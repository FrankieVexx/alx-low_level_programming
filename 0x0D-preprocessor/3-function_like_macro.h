#ifndef FUNCTION_LIKE_MACRO_H
#define FUNCTION_LIKE_MACRO_H

/*
 * File name: 3-function_like_macro.h
 * Description: A header file that defines function_like macro
 * ABS(x) that computes the absolute value of x
 * ABS(x): the file to be defined
 */

#define ABS(x) ((x) < (0) ? ((x) * (-1)) : (x))

#endif
